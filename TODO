Add promptIfIncompleteInfo to the GUI config

BUG? encoding with different genre's
Multiple Artists for 1 album.
Add the ability to encode files.
Rip and encode only 1 song at a time. (i.e. what the audioslave does now, but with a gui).
Plugin system for encoders (user friendly thingys)

If possible restart jobs that have failed.

---
FYI: Tmp files are stored in $KDEHOME/tmp-$HOST/appname which would be bad to change globally.
